# user_spec.rb の解説

## このファイルの役割

`spec/models/user_spec.rb` は、`User` モデルのテストファイルです。RSpec（Rubyのテストフレームワーク）を使って、ユーザーのバリデーション（入力値の検証）が正しく動作するかどうかを確認します。正常なデータなら保存できること、メールアドレスが空なら保存できないことを自動でテストしています。

## コード全体

```ruby
require 'rails_helper'

RSpec.describe User, type: :model do
  describe 'バリデーションのテスト' do
    context '正常系（保存できる場合）' do
      it 'すべての必須項目が正しく入力されていれば、ユーザーが保存されること' do
        user = build(:user)
        expect(user).to be_valid
      end
    end

    # ▼ ここから追加 ▼
    context '異常系（保存できない場合）' do
      it 'メールアドレスが空欄だと保存できないこと' do
        # わざとemailを空にしてダミーユーザーを作る
        user = build(:user, email: '')
        # userが valid? ではない（無効である）ことを期待する
        expect(user).not_to be_valid
      end
    end
    # ▲ ここまで追加 ▲
  end
end
```

## 行ごとの解説

| 行番号 | コード | 解説 |
|--------|--------|------|
| 1 | `require 'rails_helper'` | RSpec用のRails設定ファイル `rails_helper.rb` を読み込みます。テストを実行するために必要な設定が含まれています。 |
| 2 | （空行） | 読みやすくするための空白行です。 |
| 3 | `RSpec.describe User, type: :model do` | `User` モデルに関するテストグループを定義します。`RSpec.describe` はテスト対象を宣言するメソッドです。`type: :model` はモデルのテストであることをRSpecに伝えます。 |
| 4 | `describe 'バリデーションのテスト' do` | 「バリデーションのテスト」というグループを作ります。`describe` はテストの目的や対象を説明するためのブロックです。入れ子にすることで階層的に整理できます。 |
| 5 | `context '正常系（保存できる場合）' do` | 「正常系（保存できる場合）」という状況を定義します。`context` は「〜の場合」という条件を表すブロックです。ここでは正しいデータでユーザーが保存できる状況を表します。 |
| 6 | `it 'すべての必須項目が正しく入力されていれば、ユーザーが保存されること' do` | 具体的なテストケースを定義します。`it` の後に書く文字列は、テストが成功したときに表示される説明文です。「〜すること」という形で期待する動作を書きます。 |
| 7 | `user = build(:user)` | FactoryBot（テスト用データを作るライブラリ）を使って、ダミーのユーザーオブジェクトを作ります。`build` はデータベースに保存せずにオブジェクトだけを作るメソッドです。`:user` は `spec/factories/users.rb` などで定義されたFactoryを参照します。 |
| 8 | `expect(user).to be_valid` | `user` オブジェクトがバリデーションを通過すること（有効であること）を検証します。`expect(...).to` は「〜であることを期待する」という構文です。`be_valid` は `user.valid?` が `true` を返すことを確認するマッチャーです。 |
| 9 | `end` | `it` ブロックの終了です。 |
| 10 | `end` | `context '正常系...'` ブロックの終了です。 |
| 12 | `# ▼ ここから追加 ▼` | コメント行です。この後に追加されたコードの範囲を示す目印です。 |
| 13 | `context '異常系（保存できない場合）' do` | 「異常系（保存できない場合）」という状況を定義します。ここでは不正なデータでユーザーが保存できない状況を表します。 |
| 14 | `it 'メールアドレスが空欄だと保存できないこと' do` | 「メールアドレスが空欄のとき保存できないこと」を確認するテストケースです。 |
| 15 | `# わざとemailを空にしてダミーユーザーを作る` | コメント行です。次の行の説明をしています。 |
| 16 | `user = build(:user, email: '')` | FactoryBotでダミーユーザーを作りますが、`email: ''` でメールアドレスをわざと空文字に上書きします。これにより「メールが空の状態」を意図的に作り出しています。 |
| 17 | `# userが valid? ではない（無効である）ことを期待する` | コメント行です。次の行の説明をしています。 |
| 18 | `expect(user).not_to be_valid` | `user` オブジェクトがバリデーションを通過しないこと（無効であること）を検証します。`not_to` は「〜でないことを期待する」という構文です。メールが空ならバリデーションエラーになるはずです。 |
| 19 | `end` | `it` ブロックの終了です。 |
| 20 | `end` | `context '異常系...'` ブロックの終了です。 |
| 21 | `# ▲ ここまで追加 ▲` | コメント行です。追加されたコードの範囲の終わりを示す目印です。 |
| 22 | `end` | `describe 'バリデーションのテスト'` ブロックの終了です。 |
| 23 | `end` | `RSpec.describe User` ブロックの終了です。 |

## まとめ

- `spec/models/user_spec.rb` はUserモデルのバリデーションが正しく動作するかをRSpecで自動テストするファイルです。
- `describe` と `context` を使ってテストを階層的に整理することで、「何のテストか」「どの状況か」が読みやすくなります。
- `build(:user)` はFactoryBotを使ったテスト用データ作成で、データベースに保存せず高速にテストできます。
- `expect(...).to be_valid` と `expect(...).not_to be_valid` で、有効・無効の両方のケースをテストしています。
- 正常系と異常系の両方をテストすることで、バリデーションの動作を確実に保証できます。
